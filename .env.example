# Db Connection
# Direct connection for local development
# Transaction pooler for production in Vercel
DATABASE_URL=

# Session signing (min 32 characters). Required for login.
SESSION_SECRET=

# Optional: first admin user (npm run db:seed)
# SEED_ADMIN_EMAIL=admin@localhost
# SEED_ADMIN_PASSWORD=changeme-change-me-now
# SEED_ADMIN_ENROLLMENT=admin-0
